import { USER_LOGIN } from './types';
import { fire } from '../config/fire';

export const userLogin = () => dispatch => {
    //onFireBaseUserAuthChange
    /*
    dispatch({
      type: USER_LOGIN,
      payload: user
    })
    */
}


export const userLogout = () => dispatch => {
  //onFireBaseUserAuthChange
  /*
  dispatch({
    type: USER_LOGOUT,
    payload: null
  })
  */
}